{
  "containerDefinitions": [
    {
      "name": "app",
      "image": "<docker-image-url>",
      "memory": 512,
      "cpu": 256,
      "essential": true,
      "portMappings": [
        {
          "containerPort": 80,
          "hostPort": 80
        }
      ]
    }
  ],
  "family": "app-task"
}
